{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "wait-for-mcp-todos",
      "type": "shell",
      "command": "until curl -s -o /dev/null http://localhost:9000/mcp; do sleep 0.5; done",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      },
      "problemMatcher": []
    }
  ]
}
