[project]
name = "mcp-todos-server"
version = "0.1.0"
description = "Minimal in-memory todos MCP server (Streamable HTTP)"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.4",
]

[tool.uv]
package = false
